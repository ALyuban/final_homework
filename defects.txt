При выполнении работы найдены и исправлены следующие ошибки:

Файл index.html.
1. Лишний пробел в подвале страницы. Сейчас "202 4", а должно быть "2024".
Опечатка "Тетирование", должно быть тестирование
2. Неправильно закрытые теги
- </ul вместо </ul> (в первом списке).
- </div вместо </div> (после карточки).
- </footer вместо </footer> (в подвале).
- </html вместо </html> (в самом конце).
3. Перепутаны описания команд Windows в первом списке, также в разделе "Системная информация"
4. Опечатка "Windos", а должно быть  Windows

Файл cmd.html.
1. Неправильное закрытие тегов:
- </ul > — лишний пробел перед закрывающей скобкой 
- </div > — лишний пробел
- </footer > — лишний пробел
2. &copy; 202 4. — лишний пробел внутри года (202 4)
3. В описании команд Windows CMD перепутаны функции:
- DIR — это отображение содержимого каталога (в файле: "Изменить каталог").
- CD — это смена каталога (в файле: "Отображение файлов и папок").
- DATE — это дата (в файле: "системное время").
- TIME — это время (в файле: "текущая дата").

Файл mac.html.
1. - Лишний закрывающий тег: После строки <p class="subtitle">Тетирование WEB-приложений</p> стоит еще один одиночный </p>
- Несоответствие тегов: В блоке name открыт <div>, но закрыт как </div (пропущена скобка >).
2. Орфографические ошибки:
- Основныые Интрументы → должно быть «Основные инструменты».
- Тетирование → должно быть «Тестирование».
- Windos → должно быть «Windows».
- &copy; 202 4. → лишний пробел в году (2024).

Файл git.html
1. - ls, cd, pwd, mkdir, mv — это базовые команды операционной системы для навигации по файловой системе.
- Git — это система контроля версий, и его команды выглядят иначе (git init, git add, git commit, git push и т.д.).
- Заголовок страницы вводит пользователя в заблуждение.
2. Орфографические ошибки:
- расфшифровывается → должно быть «расшифровывается».
- как"Move" → пропущен пробел после как.
- Вернуся на главную → должно быть «Вернуться на главную».
- 202 4. → лишний пробел в году (2024).
3. - Лишний закрывающий тег: Внутри <li> для команды mv в конце стоит лишний слэш: файл/.
- Неверная вложенность (Footer): Внутри тега <footer> открыт параграф <p>, но он закрыт тегом </p>, который находится после ссылки, при этом сама ссылка <a> не обернута правильно или структура нарушена.
- Путаница в путях: Ссылка <a href="mac.html"> ведет на файл в той же папке, в то время как стили и иконки подключаются через ../ (на уровень выше). Если страница находится в подпапке, ссылка может быть неверной.

